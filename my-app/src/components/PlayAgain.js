import React from 'react'

export default function PlayAgain({ isCorrect }) {
  return (
    <div>
        {isCorrect && (
            <div>
                {/* <button className="pa">TEST</button> */}
                <p className="mathl">TESTINGGGGGGGG</p>
            </div>
        )}
    </div>
  )
}

// WORK IN PROGRESS